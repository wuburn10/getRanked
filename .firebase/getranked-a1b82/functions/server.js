import { onRequest } from 'firebase-functions/v2/https';
  const server = import('firebase-frameworks');
  export const ssrgetrankeda1b82 = onRequest({"region":"asia-east1","maxInstance":1}, (req, res) => server.then(it => it.handle(req, res)));
  